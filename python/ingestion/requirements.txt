# EPA Knowledge Graph - Python Ingestion Service
# FastAPI service for PDF parsing, chunking, embedding, and ChromaDB storage

fastapi==0.115.0
uvicorn[standard]==0.34.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# PDF Processing
pymupdf==1.24.14  # PyMuPDF (fitz) - best for text extraction + TOC
pdfplumber==0.11.7  # Table extraction

# Embeddings - use openai client for both OpenRouter and Ollama (via /v1/embeddings)
openai==1.54.0  # OpenRouter-compatible client
httpx==0.28.1

# ChromaDB - use HTTP client mode to avoid PyO3 compilation
chromadb==0.4.22

# Text Processing - avoid tiktoken compilation issues
# Use character-based approximation instead
# tiktoken==0.8.0  # Commented out due to Python 3.14 incompatibility

# Utilities
pyyaml==6.0.1
rich==13.9.4  # CLI progress
tqdm==4.66.5
tenacity==8.5.0  # Retry logic

# Testing
pytest==8.3.4
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.28.1